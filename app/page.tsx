import React from 'react';
import Work from '@/components/Work';

type Props = {};

const Home = async (props: Props) => {
  return (
    <div>
      <Work />
    </div>
  );
};

export default Home;
